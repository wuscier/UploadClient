程序安装完成后，在根目录下有一配置文件ConfigResult.xml, 打开它修改其中的<ServerIp>节点，确保配置的是正确的ip。
例如  <ServerIp>http://172.16.250.206:1003/</ServerIp>。
可修改课例上传的方式ftp或者http。
例如 <UploadMode>ftp</UploadMode> 或者 <UploadMode>http</UploadMode>