namespace UploadClient
{
    public enum UploadStatus
    {
        FileNotSelected,
        FileSelected,
        StartTranscoding,
        StopTranscoding,
        Transcoded,
        StartUploading,
        //StopUploading,
        Finished
    }
}
